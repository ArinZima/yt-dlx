{
  "name": "yt-dlp",
  "version": "20.1.0",
  "types": "core/index.d.ts",
  "main": "core/index.cjs.js",
  "module": "core/index.esm.js",
  "description": "Effortless audio-video download and streaming. yt-dlp does the heavy load for free.",
  "bin": {
    "yt": "core/cli/main.js",
    "yt-dlp": "core/cli/main.js"
  },
  "scripts": {
    "test": "tsup 'core/__tests__' --outDir 'temp' && node 'temp/cjs.mjs'",
    "relink": "yarn build && yarn unlink && yarn link && yarn link yt-dlp && yarn cli",
    "cli": "yarn yt-core version && yarn yt audio-lowest --query 'PERSONAL BY PLAZA'",
    "clean": "yarn clean:frontend && yarn clean:server",
    "clean:frontend": "rm -rf frontend/node_modules frontend/.next",
    "clean:server": "rm -rf server/node_modules server/temp server/core",
    "setup": "chmod +x .devcontainer/setup.sh && .devcontainer/setup.sh",
    "spec": "rm -rf temp && tsup 'core/quick.spec.ts' --outDir 'temp' && node 'temp/quick.spec.mjs'",
    "build": "tsup --config 'tsup.config.ts' && rollup -c 'rollup.config.mjs' && cd server && rollup -c 'rollup.config.mjs'",
    "rebuild": "rm -rf node_modules core temp && cd server && rm -rf node_modules && yarn install && yarn install --cwd server && yarn upgrade --latest && yarn upgrade --latest --cwd server && yarn build",
    "upload": "yarn build && npm pkg fix && yarn publish --access=public && yarn rebuild"
  },
  "files": [
    "core",
    "LICENSE",
    "README.md",
    "yarn.lock"
  ],
  "engines": {
    "node": ">=18"
  },
  "author": {
    "name": "shovitdutta"
  },
  "licenses": [
    {
      "type": "AGPL-3.0",
      "url": "https://opensource.org/licenses/AGPL-3.0"
    }
  ],
  "homepage": "https://rebrand.ly/mixly",
  "repository": {
    "type": "git",
    "url": "https://mixly-shovitdutta.koyeb.app/pkg"
  },
  "keywords": [
    "yt-dlp",
    "ytdlp",
    "cli",
    "yt"
  ],
  "dependencies": {
    "@shovit/ytid": "^1.0.0",
    "axios": "^1.6.7",
    "body-parser": "^1.20.2",
    "colors": "^1.4.0",
    "dotenv": "^16.4.4",
    "fluent-ffmpeg": "^2.1.2",
    "minimist": "^1.2.8",
    "ora": "^8.0.1",
    "package-json": "^9.0.0",
    "readline-sync": "^1.4.10",
    "spinnies": "^0.5.1",
    "stream": "^0.0.2",
    "ytdl-core": "^4.11.5",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@rollup/plugin-json": "^6.1.0",
    "@rollup/plugin-typescript": "^11.1.6",
    "@types/async": "^3.2.24",
    "@types/chai": "^4.3.11",
    "@types/fluent-ffmpeg": "^2.1.24",
    "@types/fs-extra": "^11.0.4",
    "@types/minimist": "^1.2.5",
    "@types/node": "^20.11.19",
    "@types/readline-sync": "^1.4.8",
    "@types/spinnies": "^0.5.3",
    "chai": "^5.1.0",
    "rollup": "^4.12.0",
    "rollup-plugin-dts": "^6.1.0",
    "rollup-plugin-typescript2": "^0.36.0",
    "ts-node": "^10.9.2",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
